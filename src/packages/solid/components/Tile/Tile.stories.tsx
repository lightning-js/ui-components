import Tile from './Tile';
import theme from 'theme';

const meta = {
  title: 'Components/Tile',
  component: Tile,
  tags: ['autodocs'],
  argTypes: {
    
  }
};

export default meta;

export const Basic = {
  args: {
    
  }
};
